# Function that runs backup script and then returns to working directory.
backup () {
 cd ~
 if [[ $1 != '' ]] then
  sh backup.sh $1
 else
  sh backup.sh
  echo 'Commit message needed for pushing to github!!'
 fi
 cd -
}
